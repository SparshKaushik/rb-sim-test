name: runnerbox

on:
  workflow_dispatch:

jobs:
  runnerbox:
    runs-on: macos-latest
    timeout-minutes: 350
    steps:
      - name: RunnerBox agent
        uses: TouchTechClub/runner@v1
        with:
          token: ${{ secrets.RUNNERBOX_TOKEN }}
