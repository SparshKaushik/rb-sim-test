name: runnerbox

on:
  workflow_dispatch:

jobs:
  runnerbox:
    runs-on: xcode-27
    timeout-minutes: 350
    steps:
      - name: RunnerBox agent
        uses: TouchTechClub/runner@v1
        with:
          token: ${{ secrets.RUNNERBOX_TOKEN }}
